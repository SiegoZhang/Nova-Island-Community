# 新岛产品原型

## 唯一入口

- 设计系统：`../design-system/index.html`，所有规范、组件示例、课程和文章示例以及配套资源仅在 `design-system/` 维护。
- 产品原型：`prototype/index.html`，使用 prototype 内独立维护的页面和资源。

本目录 `index.html`、`course.html`、`article.html` 仅为旧地址兼容跳转，保留查询参数和锚点，不含设计系统副本。原重复 assets 目录已移除。`home.html` 保留原型旧预览兼容入口。

设计系统说明见 `../design-system/NOTES.md`；产品原型说明见 `prototype/README.md`。

## 文案约束

词语或短语之间不使用居中圆点作为分隔符。并列信息使用独立元素与 CSS gap，句子使用自然语言或逗号。页面标题、导航、按钮、标签及动态文案均遵守此规则。
